# ORCHESTRA.md

Reserved for Orchestra's native workflow contract once Orchestra runs itself.

For now:
- `WORKFLOW.md` is the active Symphony runtime contract
- Orchestra-native behavior lives in `docs/`
- do not infer future Orchestra semantics from Symphony-specific hook behavior
